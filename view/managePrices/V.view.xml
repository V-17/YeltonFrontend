<mvc:View controllerName="controller.managePrices" xmlns:l="sap.ui.layout" xmlns:mvc="sap.ui.core.mvc" xmlns="sap.m">
    <Page showHeader="false">
        <subHeader>
            <Toolbar>
                <ToolbarSpacer/>
                <Button
                    id="buttonSettings"
                    icon="sap-icon://settings"
                    text="{user>/login}"
                    tooltip="Меню"
                    press="onSettingsButtonPress"/>
            </Toolbar>
        </subHeader>
        <Table id="tablePrices" mode="SingleSelectMaster" items="{/}" fixedLayout="false">
            <headerToolbar>
                <Toolbar>
                    <Button icon="sap-icon://add" press="onCreateButtonPress" tooltip="Создать"/>
                    <Button icon="sap-icon://edit" press="onEditButtonPress" tooltip="Изменить"/>
                    <Button icon="sap-icon://delete" press="onDeleteButtonPress" tooltip="Удалить"/>
                    <ToolbarSpacer/>
                </Toolbar>
            </headerToolbar>
            <columns>
                <Column/>
                <Column/>
                <Column/>
            </columns>
            <items>
                <ColumnListItem type="Active">
                    <cells>
                        <ObjectIdentifier title="{productName}" text="{categoryName}"/>
                        <ObjectIdentifier title="{price} {currencyShortName}" text="{amount} {unitShortName}"/>
                        <ObjectIdentifier
                            title="{storeName}"
                            text="{
                                path: 'date',
                                type: 'sap.ui.model.type.Date',
                                formatOptions: {
                                    style: 'long',
                                    source: {
                                        pattern: 'dd.MM.yyyy'
                                    }
                                }
                            }"/>
                    </cells>
                </ColumnListItem>
            </items>
        </Table>
    </Page>
</mvc:View>
